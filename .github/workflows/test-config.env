# Test environment configuration for CI/CD
# This file contains test-specific overrides for environment variables

# Database configuration for testing
DB_HOST=localhost
DB_PORT=5432
DB_NAME=regulens_test
DB_USER=regulens_test
DB_PASSWORD=test_password
DB_SSL_MODE=disable

# Test-specific settings
REGULENS_ENVIRONMENT=test
LOG_LEVEL=debug
ENABLE_METRICS=false

# Mock external services for testing
VECTOR_DB_HOST=localhost
VECTOR_DB_PORT=8080
MESSAGE_QUEUE_BOOTSTRAP_SERVERS=localhost:9092
COMPLIANCE_MODEL_ENDPOINT=http://localhost:8501/v1/models/compliance_model
